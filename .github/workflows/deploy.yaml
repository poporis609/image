name: Build and Push to ECR

on:
  push:
    branches:
      - main

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: image-generator

permissions:
  contents: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image to Amazon ECR
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker tag $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_REPOSITORY:latest
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest

      - name: Update k8s deployment with secrets
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
          KNOWLEDGE_BASE_BUCKET: ${{ secrets.KNOWLEDGE_BASE_BUCKET }}
          BEDROCK_FLOW_ID: ${{ secrets.BEDROCK_FLOW_ID }}
          BEDROCK_FLOW_ALIAS_ID: ${{ secrets.BEDROCK_FLOW_ALIAS_ID }}
          ACM_CERTIFICATE_ARN: ${{ secrets.ACM_CERTIFICATE_ARN }}
        run: |
          # Update deployment.yaml
          sed -i "s|\${ECR_REGISTRY}|$ECR_REGISTRY|g" k8s/deployment.yaml
          sed -i "s|image:.*|image: $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG|" k8s/deployment.yaml
          sed -i "s|\${KNOWLEDGE_BASE_BUCKET}|$KNOWLEDGE_BASE_BUCKET|g" k8s/deployment.yaml
          sed -i "s|\${BEDROCK_FLOW_ID}|$BEDROCK_FLOW_ID|g" k8s/deployment.yaml
          sed -i "s|\${BEDROCK_FLOW_ALIAS_ID}|$BEDROCK_FLOW_ALIAS_ID|g" k8s/deployment.yaml
          
          # Update ingress.yaml
          sed -i "s|\${ACM_CERTIFICATE_ARN}|$ACM_CERTIFICATE_ARN|g" k8s/ingress.yaml

      - name: Commit and push updated deployment
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add k8s/
          git diff --staged --quiet || git commit -m "[skip ci] Update image tag to ${{ github.sha }}"
          git push
